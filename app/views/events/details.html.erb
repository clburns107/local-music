<h1>Details Page</h1>
<p>This page displays a list of details for the event that you clicked on, including venue information and band information</p>

Loop through all events on this day and display venue, bands, and time
<p>
  <%if @events_on_this_day == nil %>
    <p>No events are scheduled for this day yet.</p>
  <% elsif %>
    <% @events_on_this_day.each do |event| %>
      <% if event.venue.name != @name_check %>
        <br><%= event.venue.name %><br>
        <%= event.band.name %> : 
        <%= event.time %><br>
      <% else %>
        <%= event.band.name %> : 
        <%= event.time %><br>
      <% end %>
      <% @name_check = event.venue.name %>
    <% end %>
  <% end %>
</p>

<p>
  <a href="/">Calendar</a>
</p>